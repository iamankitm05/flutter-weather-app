import 'package:flutter/material.dart';

abstract final class AppColors {
  static const darkPrimary = Color(0xff00020f);
  static const darkSecondary = Color(0xff10121c);
  static const lightPrimary = Color(0xffe1e4fc);
  static const lightSecondary = Color(0xffcccedb);
  static const black = Colors.black;
  static const white = Colors.white;
  static const deepPurple = Colors.deepPurple;
}
